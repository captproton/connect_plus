<div class="site-header" id="header">
    <div class="main-header">
        <div class="header-logo">
            <%= render "sites/shared/inline_edit" , model: @site, method: :logo_url do %>
            
                <% if !@site.logo_url.present? %>
                    <svg height="50" width="50">
                        <circle cx="25" cy="25" r="20" stroke="black" stroke-width="3" fill="red" />
                    </svg>
                <% else %>
                    <%= image_tag(@site.logo_url, width: "40") %>
                <% end %>
            <% end %>


            <%= render "sites/shared/inline_edit" , model: @site, method: :name do %>
                <span>
                    <%= @site.name || "Your name here" %>
                </span>
            <% end %>

                
        </div>

        <nav class="wizrd-site-navigation wizrd-site-navigation-center false">
            <ul>
                <li><a href="#">Home</a></li>
                <li><a href="#">About</a></li>
                <li><a href="#">Contact</a></li>
            </ul>
        </nav>        
        <nav class="site-navigation site-navigation-social">
            <!-- START -->
            <ul>
                <!-- <li class="mobile-nav">
                    <a id="home-submenu" aria-expanded="false" href="/" target="_self">home</a>
                </li>
                <li class="mobile-nav">
                    <a id="contact-submenu" aria-expanded="false" href="/contact" target="_self">contact</a>
                </li> -->
                <!--  linkedin -->

                <!--  youtube -->

                <!--  tiktok -->

                <li>
                    <button type="button" class="btn btn-primary author-subscription-action" data-bs-toggle="modal"
                        data-bs-target="#subscribeModal" onclick="closeModal()">Subscribe</button>
                </li>
            </ul>
            <!-- FINISH -->
            
        </nav>
    </div>
</div>